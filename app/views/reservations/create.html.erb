<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h2>Congrats, you rent a book!</h2>
    </div>
  </div>

  <div class="row">


    <div class="col-xs-12 col-sm-6 col-md-4">
      <%= render 'shared/collection_item_card', collection_item: @reservation.collection_item %>
    </div>

    <div class="col-xs-12 col-sm-6 col-md-4">
      <div id="reservation-recap">
        <h3>Reservation's recap :</h3>
        <p><strong>Status : <%= @reservation.status %> </strong>- The owner of the book has to confirm your reservation first :) </p>
        <p>Price : <%= @reservation.final_price.to_f / 100 %> €</p>
        <p>Rented from <%= @reservation.start_day %> to <%= @reservation.end_day %></p>
        <p>Owner : <%= @reservation.collection_item.profile.first_name %></p>
        <%= link_to dashboards_my_rentals_path do %>
          <div class="btn btn-primary">Go to my reservations</div>
        <% end %>
      </div>
    </div>
  </div>
</div>
